
set_int_vec_near:

    push    es
    push    bx

    push    ax
    xor     ax,   ax
    mov     es,   ax
    shl     bx,   2
    mov     ax,   cs
    mov     [es: bx+2], ax
    pop     ax
    mov     [es: bx+0], ax
    
    pop     bx
    pop     es
    ret